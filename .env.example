# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# PDF knowledge: local development defaults to server_data/knowledge (gitignored).
# For production, choose ONE durable backend. Cloud storage uses the server's
# Application Default Credentials; never place service-account keys in git.
# PLASGAIN_KNOWLEDGE_BUCKET="your-private-gcs-bucket"
# PLASGAIN_KNOWLEDGE_DIR="/mounted-persistent-disk/knowledge"

# Isolated local testing only (disable both server and browser Firestore sync).
# PLASGAIN_DISABLE_CLOUD=1
# VITE_PLASGAIN_DISABLE_CLOUD=1

# GEMINI_MODEL: Optional. Overrides the default Gemini model without a code change.
# Defaults to gemini-3.8-flash. If the id is not enabled for your key the server
# falls back down the ladder (3.7-flash, 2.5-flash, 2.0-flash) rather than failing.
# GEMINI_MODEL="gemini-3.8-flash"
