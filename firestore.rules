rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all authenticated or application CRM collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
