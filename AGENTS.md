# Project Instructions & Workflow Rules

- **Always Push to GitHub**: Whenever any code or configuration changes are made, tested, or completed, always commit and immediately push to GitHub (`origin/main`).
- **Secrets Management**: Never commit secrets or `.env` files to git. Keep `.env*` ignored (except `.env.example`).
